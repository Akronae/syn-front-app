export interface Theme {
  colors: {
    text: {
      primary: string
    }
  }
  typography: {
    font: {
      light: string
      regular: string
      bold: string
    }
    size: {
      xxl: number
      xl: number
      lg: number
      md: number
    }
  }
}
