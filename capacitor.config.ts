import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.syneidesis.client',
  appName: 'Syneidesis',
  webDir: 'www',
  bundledWebRuntime: true
};

export default config;
