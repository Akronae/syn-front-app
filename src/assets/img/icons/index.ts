const icons = {
  interface: {
    error: {
      input: require(`./interface/error/input.svg`),
    },
    pointer: require(`./interface/pointer.svg`),
    add_bank: require(`./interface/add_bank.svg`),
  },
}

export default icons
